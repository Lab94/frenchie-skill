---
description: Convert a Word, Excel, CSV/TSV, or PowerPoint file into Markdown through Frenchie.
---

Use the `extract_to_markdown` MCP tool for `$ARGUMENTS`.

If the MCP server is HTTP, upload first with `upload_file` and pass `uploaded_file_reference`.
If the MCP server is stdio, pass the absolute local path as `file_path`.
